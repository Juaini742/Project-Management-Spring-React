import MainTemplate from "@/components/template/MainTemplate.tsx";

export default function ProfilePage() {
    return (
        <MainTemplate>
            <h1>Profile Page</h1>
        </MainTemplate>
    );
}